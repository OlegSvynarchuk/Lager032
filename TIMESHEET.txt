LAGER032 — WORK LOG / TIMESHEET
Project: WordPress + WooCommerce migration & custom theme (lager032)
Site: https://lager032.pixels2pixels.ch  (dev)


=====================================================================
2026-06-15
=====================================================================
- Published the full catalogue: set all 4,934 products from draft to
  PUBLISHED; turned on "discourage search engines from indexing" so the
  dev site stays out of Google.
- Built the SINGLE PRODUCT page (data-first template): breadcrumb,
  category image used as an illustrative reference, title, code (sifra),
  brand, price, stock, quantity + add-to-cart, "call for enquiry" CTA,
  specifications table, related products, and Product + Breadcrumb
  structured data (JSON-LD for SEO).
- Brand filter: decided to SKIP for now (brand is not in the source data;
  only ~half of product names contain a brand, with false positives) — the
  manufacturer facet auto-hides.
- URL ARCHITECTURE: moved from query-string URLs to clean Serbian
  permalinks — /prodavnica/ (shop), /kategorija/{slug}/ (categories),
  /proizvod/{slug}/ (products); created the missing server .htaccess so the
  pretty URLs resolve (decided against a shop. subdomain for SEO).
- Set "best SEO practices" as a standing project principle.
- Layout: tightened the product-archive top spacing.

=====================================================================
2026-06-16
=====================================================================
- HEADER layout: right-aligned the top contact bar (phones beside e-mail
  + address); centered the "Svi proizvodi" + main navigation block in the
  header; added a "Katalog" menu item; widened nav spacing; switched the
  logo to the image logo.
- MEGA-DROPDOWN: added subcategory fly-out menus on hover for the
  categories that have subcategories (driven live from the catalogue;
  collapses to the parent so it stays clean).
- Built LIVE AJAX SEARCH (typeahead):
    * searches product name AND code/SKU, ranked, diacritic-insensitive;
    * code normalization (6205-2RS = 6205 2RS = 62052rs all match);
    * category suggestions (collapsed to the matched parent, real counts);
    * grouped + labelled results (Kategorije / Proizvodi), highlighted match;
    * quick add-to-cart from the results + live cart badge;
    * keyboard navigation, "view all results" link, no-results state.
- PRICING: switched the whole storefront to show the FULL price including
  VAT (PDV) instead of the net price (removed the "bez PDV-a" display) —
  e.g. 1.599 -> 1.919 RSD; updated product structured data accordingly.
- Diagnostics: analysed product naming vs. categorisation (the "spojnica"
  gap report) to confirm search behaviour.
- Updated project documentation (PROGRESS.md); committed and deployed all
  changes to the dev site.

=====================================================================
2026-06-17
=====================================================================
- PRODUCT NAMES: cleaned the "---" separator left over from the export out
  of 176 product names (e.g. "BETA OSIGURAC 3X80 ---" -> "BETA OSIGURAC
  3X80"). Page addresses unchanged.
- HEADER: fixed the navigation so it no longer overlaps the search box on
  smaller screens (it now centers in the available space and collapses to
  the menu button earlier when there isn't room).
- SHOP/CATEGORY FILTERING REWORKED for clarity + SEO:
    * Categories are now NAVIGATION, not a filter — the left panel shows
      top-level categories on the shop and only the sub-categories on a
      category page (nothing on a category with no sub-categories), so you
      can never pick a combination that returns an empty list.
    * Added breadcrumbs (with search-engine structured data) to move back
      up to the shop / parent category.
    * Search, availability and price filters stay the same on every page.
- SEO: filtered/sorted URLs are now "noindex" and point back to the clean
  category/shop page (canonical), so search engines index the clean pages
  only — protects rankings.
- SHOP UX: added removable "active filter" chips and kept the scroll
  position when applying a filter (no more jumping to the top).
- Committed and deployed everything to the dev site.
- FILTERING CONSISTENCY pass:
    * Category sidebar is now a persistent, collapsible list of links (open
      on the current branch) — one click to any category from any page.
    * Moved the Filters box above the category list.
    * Price filter is now a slider with two handles using the real lowest/
      highest prices of the CURRENT list; updates as you filter; applies when
      you release; hides itself when there is no range (1 item / one price).
    * Fixed a confusing redirect: searching down to a single product used to
      jump you to that product page — now it stays on the list.
    * Made the shop/category search find the same products as the instant
      search box (codes like 6204-2RS / 62042RS now match everywhere).
    * Fixed stray spacing above the header.
- Added a quantity stepper + instant add-to-cart on each product row
  (set amount, click "Dodaj" → added straight to the cart, badge updates).
- Fixed the same stray-spacing bug on the single product page.
- Deployed everything to the dev site.

=====================================================================
2026-06-18
=====================================================================
- Reviewed and verified the previous day's work on the dev site (clean
  URLs + SEO noindex/canonical, category navigation + breadcrumbs, price
  slider, filter chips, title cleanup, per-row add-to-cart) — all working.
- CART STATE ON THE PRODUCT LIST: the shop list now reflects the cart on
  page load — products already in the cart are highlighted and their row
  quantity matches the cart. The row button now UPDATES the cart to the
  chosen quantity (set, not blind re-add), removes at 0, and caps to the
  available stock. Built cache-safely (applied client-side so the shop
  pages stay cacheable). Tested end-to-end on the dev site.
- STOCK NO LONGER BLOCKS SALES (per client): customers can order ANY
  quantity regardless of the database stock, including items at 0 — orders
  are processed manually and stock is refilled. Removed the quantity cap and
  made every product always orderable (no per-product data change). Removed
  the "out of stock" state + exact stock counts from the storefront (stock
  numbers remain in admin for reference); products show simply "Na stanju".
- SINGLE PRODUCT add-to-cart now behaves like the list: AJAX (no page
  reload), the button is marked after adding ("U korpi (N)"), it reflects
  the cart on page load, and has a remove button. (Previously it was a plain
  form submit with no on-page feedback.)
- MINI-CART DRAWER: clicking the cart icon in the header now opens a slide-out
  cart panel (built to match the theme) instead of leaving the page — shows
  the items (image, name, qty × price, line total), a running subtotal, and
  "Pogledaj korpu" + "Plaćanje" buttons; remove an item inline; updates live
  as you add from the list/search. Keeps the shopper on the catalogue.
- Also added a "sa PDV-om" label next to every price (clarifies the displayed
  price includes VAT).
- DRAWER controls: each item now has a +/− quantity stepper (updates the cart
  and totals live) and a remove button, plus an "Isprazni korpu" (empty cart)
  action — manage the whole cart from the drawer without leaving the page.
- CURRENCY UNIFIED to Serbian Latin: prices showed the Cyrillic "рсд" while the
  rest of the site is Latin — switched the currency symbol to "RSD" everywhere
  so the storefront reads consistently.
- CHECKOUT & CART rebuilt as a SINGLE STYLED CHECKOUT:
    * The slide-out drawer is now the cart; "Plaćanje" goes straight to one
      themed checkout page (which already lists the order) — fewer steps.
    * Converted cart & checkout from Gutenberg blocks to the classic forms so
      the theme, fields and text are fully under our control.
    * Enabled "Uplatnica / bankovni transfer" payment and created a "Srbija"
      shipping zone (courier flat-rate + free delivery over a threshold).
      NOTE: bank account number, shipping price and free-shipping threshold are
      PLACEHOLDERS — need the real values from the client.
    * Trimmed the form for retail buyers (removed company / 2nd address line /
      region, made phone required) and translated all checkout & cart wording to
      Serbian Latin (labels, buttons, headings, messages).
    * Styled the whole checkout + cart to the site theme: two-column layout with
      a sticky order summary, themed inputs, buttons, totals and notices.
    * Card payments online are PENDING — they need a Serbian payment gateway and
      a merchant account (e.g. WSPay / Monri / AllSecure-Intesa). The checkout is
      ready to add it once the client chooses a provider.
- CHECKOUT REBUILT to match the client's existing site (one page):
    * Cart + checkout are now a SINGLE page at /korpa/ — customer details +
      delivery details on top, the full cart table (Šifra, Kategorija, Naziv,
      Cena, Količina, Ukupno) with editable quantity + remove below, then the
      Osnovica / PDV / "Ukupno za naplatu" summary. Opening the cart lands here.
    * Form fields match the old site: Ime, Prezime, E-mail, Mobilni telefon,
      Fiksni telefon / Ulica, Broj zgrade/kuće, Sprat, Broj stana, Interfon,
      Grad, Dostavna poruka. Extra fields are saved on the order + shown in the
      admin and order e-mails. Country fixed to Srbija (no picker).
    * SHIPPING is no longer calculated — the page states "Troškove dostave plaća
      kupac" (buyer pays the courier), as on the old site.
    * Removed the page title ("Cart"), the coupon/login boxes, and all the
      unused B2B fields. Everything is in Serbian Latin.
    * Fixed the broken header on cart/checkout (the logo was blown up huge and
      the bar was too tall) — it now matches every other page.
    * Fully restyled to the theme. Note: cart/checkout page slug is now /korpa/.
- CHECKOUT LAYOUT polish: the page was stretching the full screen width; put it
  in a centered container and arranged it in two columns — customer/delivery
  forms on the left, the cart + order summary on the right (stacks on phones).
- PAYMENT METHOD SELECTION: added a second method so buyers can choose — checkout
  now shows selectable payment cards (radio buttons): "Uplatnica / bankovni
  transfer" and "Plaćanje pouzećem" (cash on delivery). (Card online still
  pending a Serbian gateway + merchant account.)
- Fixed the order total appearing twice on the checkout: the per-line total
  equalled the grand total on single-item orders, and a WooCommerce on-load
  refresh was leaving a second copy of the totals block behind. Removed the
  duplicate line-total column and moved the totals inside the order table so the
  live refresh swaps them as one unit — the total now shows exactly once.
- Removed a leftover English "(incl. VAT)" tag from prices.
- Verified the whole order flow end-to-end: test orders placed successfully and
  arrived correctly in the dashboard.
- DASHBOARD IN SERBIAN: switched the WordPress admin to Serbian for the content
  manager — installed the WordPress + WooCommerce Serbian language pack and
  applied it per account, so the public storefront is completely untouched
  (still Latin). NOTE: WordPress/WooCommerce only ship Serbian in Cyrillic, so
  the dashboard is in Cyrillic; it's reversible per user at any time.
